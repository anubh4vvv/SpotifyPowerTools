# Packaging Spotify Power Tools

Spotify Power Tools can be packaged as a Windows desktop app using PyInstaller.

## Requirements

Install project dependencies first:

```bash
pip install -r requirements.txt